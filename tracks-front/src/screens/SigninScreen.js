import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SigninScreen = () => {
  return <Text style={{ fontSize: 30 }}>SigninScreen</Text>;
};

const styles = StyleSheet.create({});

export default SigninScreen;
